#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes absolute value
 *
 * Description: Computes an absolute value of a passed interger
 *
 * @a: The integer to be computed to an absolute integer
 *
 * Return: Integer value of absolute value of n
 */
int _abs(int a)
{
	int absolute = abs(a);

	return (absolute);
}
