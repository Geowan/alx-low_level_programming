#include "main.h"

/**
 * mul- Multiplies two integers
 *
 * @a: first integer passed into the function
 * @b: Integer passed into the function
 * Return: multiplication result
 */
int mul(int a, int b)
{
	int result = (a * b);

	return (result);
}
